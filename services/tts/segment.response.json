{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://image.a11y.mcgill.ca/services/tts/segment.response.json",
    "title": "TTS Segment Response",
    "type": "object",
    "description": "Response of timestamped audio segments.",
    "type": "object",
    "definitions": {
        "durations": {
            "description": "Duration of each segment in the audio file.",
            "type": "array",
            "items": {
                "description": "Duration of the segment in samples.",
                "type": "integer",
                "minimum": 0
            }
        }
    },
    "properties": {
        "audio": {
            "description": "Data URL of the base64 wave file returned.",
            "type": "string",
            "pattern": "^data:audio/wav;base64,[a-zA-Z0-9+/=]+$"
        },
        "durations": {
            "$ref": "#/definitions/durations"
        }
    },
    "required": [
      "audio",
      "durations"
    ]
}

{
  "name": "hello-preprocessor",
  "version": "0.0.1",
  "description": "Example of a preprocessor service.",
  "main": "dist/server.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node ."
  },
  "dependencies": {
    "ajv": "^8.3.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^15.0.2",
    "tslint": "^6.1.3",
    "typescript": "^4.2.4"
  }
}
